# Terminal

## Node

#### (force) clear you npm cache
```bash
sudo npm cache clean -f
```

#### install "n"
```bash
sudo npm install -g n
```

#### upgrade to latest version
```bash
sudo n stable
```

#### check version
```bash
node -v
```
